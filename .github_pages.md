# GitHub Pages 发布（可直接打开玩）

1. 把仓库推到 GitHub。
2. 在仓库 Settings → Pages。
3. Build and deployment 选择 **Deploy from a branch**。
4. Branch 选择你的分支（如 `main`），目录选择 `/web`。
5. 保存后等待 1~3 分钟，得到链接：
   - `https://<你的用户名>.github.io/<仓库名>/`

本项目的可玩入口文件为：`web/index.html`。
