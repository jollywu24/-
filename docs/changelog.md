# 更新记录

## 2026-06-06

### 当前状态

- 上头值最大值调整为 `160`，达到 `160` 时爆牌。
- 红眼改为 `100` 进入、`80` 或以下退出，并在红眼状态下提供本手倍率 `×1.5`。
- 普通关、精英关、Boss 关过关减压分别调整为 `25`、`35`、`50`。
- 商店加入可花费赌资购买的降压道具。
- 商店赌鬼池替换为 5 张已完整接入结算的红眼流派赌鬼牌：血丝眼镜、红眼借据、小牌壮胆、烂命保险、戒断反弹。
- 移除商店中未接入实际结算的旧赌鬼牌。
- 红眼改为全局视觉状态，加入普通、预热、红眼、临界、下注和爆牌状态 class。
- 补充进入/退出红眼、临界、爆牌短动画，并实现暗涌牌从右上牌堆飞出、翻面和上头值飞入表盘的结算动画。
- 赌鬼改变红倍率时，会在对应赌鬼牌下方弹出带暗红底图的倍率飘字。
- 初始化时不再显示默认赌鬼卡。
- 顶部赌鬼区保留位置，但初始计数为 `0 / 5`，只在获得赌鬼后显示具体卡牌。
- 红眼赌注弹窗按效果图方向还原为暗红边框、顶部红眼装饰、三张赌注卡和底部选择提示。

## 2026-06-05

### 文档整理

- 对齐当前主界面规范：五个常驻区域保持不变，红眼选择、通关结算和失败结果只作为事件触发弹窗出现。
- 补充当前红眼赌注池、暗涌牌、通关收益结算和失败弹窗规则。
- 更新 MVP 状态：通关收益弹窗和失败弹窗已接入，盲注之间的正式商店/提现步骤仍未接入。
- 将稳定规则集中到 `ui_spec.md` 和 `component_rules.md`，`changelog.md` 只保留变更摘要，避免重复维护同一套规则。

## 2026-06-03

### 当前状态

- 手牌在初始化发牌、换牌补牌、摊牌补牌和下一轮重新发牌后会自动按点数和花色排序。
- 摊牌时，已选牌按当前手牌显示顺序进入中央出牌区。
- 结算时只对牌型有效牌触发单牌筹码表现，高牌等牌型不会让无效牌重复计分。
- 出牌区筹码飘字改为单个蓝底数字，跟随对应出牌上方居中显示。
- 弃牌和出牌清场统一向右飞出屏幕外，补牌由牌堆飞向手牌区。
- 初始化发牌和补牌使用牌背飞行动画，接近手牌位置后翻成正面，收尾不再闪烁或跳位。

### 设计经验

- 手牌排序是可读性规则，不是抽牌规则。只应改变手牌展示，不应影响牌堆、弃牌堆或 seed。
- 飞牌动画要用克隆牌承载运动，真实手牌只负责最终状态；两者切换时必须冻结过渡，避免明暗闪烁。
- 计分动画应绑定到真正计分的牌，未参与牌型的牌保持安静，玩家才容易理解为什么加了这些筹码。

## 2026-05-30

### 当前状态

- 网页 MVP 已从早期插槽/模块原型转向扑克牌型原型。
- 基础牌使用真正的 52 张牌组，点数为 `2-10 / J / Q / K / A`。
- 已实现抽牌堆和弃牌堆。
- 已实现标准扑克牌型识别。
- 当前 UI 已接入选牌、摊牌、换牌、目标分数、摊牌次数和上头值。
- 底层逻辑已支持赌鬼/Joker 修正、牌型升级和 Boss 负面规则，但当前主 UI 尚未完整接入商店、卡包、Boss 流程。
- 初始局不再免费携带 Joker 风格修正。
- 选中牌与已提交出牌分离：选中手牌不会立刻复制到出牌区。
- 可以通过 `?seed=` 复现随机序列。
- 过关行为会保留 `上头值`，并按普通关、精英关、Boss 关分别减少 `25`、`35`、`50`，重置已选择的 `红眼赌注`。
- 失败优先级为：`上头值 >= 160` 立即爆牌，优先于胜利；摊牌次数耗尽且未达标时触发 `庄家通吃`。

### UI 变化

- 倍率结算改为按来源 step 顺序播放：来源牌反馈、红倍率滚动、当前分数跟随跳动，并区分加法、乘法和暗涌的演出强度。
- 主界面重新聚焦为五个常驻区域：
  - 左侧信息栏
  - 顶部赌鬼区
  - 中央出牌区
  - 底部手牌区
  - 右侧操作区
- 左侧 HUD 围绕目标分数、当前分数、当前牌型、筹码 × 倍率、赌资/筹码、上头值和剩余操作重新设计。
- 当前视觉方向变为黑暗赌桌、旧纸牌、赌鬼卡、红眼和锁定的红眼赌注。
- 主游玩界面移除了常驻商店、日志和额外面板。
- 主 UI 画布使用固定 `1600 x 900` 等比缩放。
- 当前效果图目标使用纯 DOM/CSS 制作，不使用外部图片、外部字体、框架或画布渲染。

### 设计经验

- UI 信息必须服务核心循环：玩家能否过关、还剩几次操作、上头值有多危险、之后有多少经济空间。
- 预览和结算要分离。预览帮助玩家决策；结算应该逐步揭示牌型、单牌得分、修正触发和风险变化。
- 选中的手牌和已提交的出牌不能视觉混在一起。
- 基础牌必须保持简单。复杂度属于赌鬼修正、Boss 规则、商店、卡包和红眼赌注。
- 静态赌鬼卡不应比主动出牌区域更亮。
- 还原参考图时，信息结构和屏幕比例比单个边框装饰更重要。

### 验证习惯

- 语法检查：`node --check web/app.js`。
- 逻辑测试：`node --test web/tests/logic-pure.test.mjs`。
- Diff 空白检查：`git diff --check`。
- 使用固定 seed 做平衡和 UI 回归检查。

### 下一步建议

1. 用多个固定 seed 测试玩家是否会自然追逐高 `上头值`。
2. 强化红眼进入、退出和爆牌阈值的反馈。
3. 增加 DOM 级回归检查，覆盖选择、换牌、摊牌、过关、爆牌失败和红眼赌注。
4. 校准目标曲线、上头值增长、过关减压和赌鬼修正强度。
